# Keycloak Placeholder

`platform/keycloak` will contain Keycloak realm bootstrap and client
configuration as code.

This placeholder keeps the planned authentication boundary visible without
introducing auth setup in the repo bootstrap unit.
