# Unit 07: Setup logger

## Goal

we have ui and api interacting so far. lets add db layer to it. the data should then be fetched and stored the actual db. 
use docker image to spin up the database locally so that I dont have to download it locally. 
## Design
