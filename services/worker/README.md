# Worker Placeholder

`services/worker` will host asynchronous job consumers and scheduled work.

Phase 0 keeps this directory as a placeholder only. No queue consumers, retry
logic, or job runtime are bootstrapped yet.
