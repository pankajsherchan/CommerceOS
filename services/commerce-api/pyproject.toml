[project]
name = "commerce-api"
version = "0.1.0"
description = "CommerceOS commerce API package skeleton."
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["uv_build>=0.11.7,<0.12.0"]
build-backend = "uv_build"
